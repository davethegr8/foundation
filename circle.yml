machine:
  php:
    version: 5.6.22

# dependencies:
#   pre:
#     - apt-get install jq

test:
  override:
    - composer test
  post:
    - cat docs/coverage/coverage.txt
    - ./coverage.php
    - cp docs/coverage/coverage.txt $CIRCLE_ARTIFACTS

